{
  "buildCommand": "npm ci --legacy-peer-deps -C client && npm run build -C client",
  "outputDirectory": "client/dist/inventory-client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
